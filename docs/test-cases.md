# Test Cases

## Purpose

This document catalogs the tests that should exist for health-export-cli.
Each test case has a stable ID, a short description, and an expected result so it can be understood without opening source files.

## Test Conventions

- Automated tests shall be written as Rust integration tests under `tests/`.
- Automated tests shall run against the static fixture archive at `tests/fixtures/export.zip` rather than ad hoc inline XML strings or the large example export.
- The plain-text source files used to build the static fixture shall remain in `tests/fixtures/` alongside the generated ZIP so fixture changes are reviewable.
- When the fixture contents change, regenerate `tests/fixtures/export.zip` with `./scripts/regenerate-test-export.sh` instead of editing the archive directly.
- Automated tests should exercise the application through the high-level Rust entry point `health_export_cli::run_from_args(...)` with injected stdout/stderr writers, rather than calling lower-level parsing helpers directly.
- Test names for automated cases shall include the corresponding stable test-case ID, for example `tc_cli_001_*`.
- New automated coverage should prefer user-visible command behavior over internal helper behavior unless a lower-level test is strictly necessary.

## Enumerated Test Cases

### CLI

| ID         | Description                                  | Precondition                                                                 | Actions                                                                                  | Expected Result                                                                                         | Automated |
| ---------- | -------------------------------------------- | ---------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- | --------- |
| TC-CLI-001 | List output is chronologically ordered       | Static test fixture ZIP with seven running workouts stored out of order in XML. | Call `--file tests/fixtures/export.zip running list`.                                    | The Markdown table lists only running workouts, ordered by `startDate`, with stable global 1-based run IDs. | ✅ `tc_cli_001_list_orders_workouts_chronologically` |
| TC-CLI-002 | Filter workouts by year                      | Static test fixture ZIP with workouts in 2023, 2024, and 2025.               | Call `--file tests/fixtures/export.zip running list --year 2024`.                       | Output includes only the 2024 workouts.                                                                 | ✅ `tc_cli_002_filters_by_year` |
| TC-CLI-003 | Filter workouts by date range                | Static test fixture ZIP with a workout on `2025-02-01` and other dates outside that month. | Call `--file tests/fixtures/export.zip running list --from 2025-02-01 --to 2025-02-28`. | Output includes the February 2025 workout and excludes workouts outside the inclusive range.            | ✅ `tc_cli_003_filters_by_date_range` |
| TC-CLI-004 | No matches prints a clear success message    | Static test fixture ZIP with no workouts in 2022.                            | Call `--file tests/fixtures/export.zip running list --year 2022`.                       | The tool prints `No running workouts found for the given time range.` and exits successfully.           | ✅ `tc_cli_004_reports_no_matches` |
| TC-CLI-005 | Out-of-range show index errors               | Static test fixture ZIP with seven matching workouts.                        | Call `--file tests/fixtures/export.zip running show 999`.                               | The tool exits with an error naming the valid range `1–7`.                                              | ✅ `tc_cli_005_show_errors_for_out_of_range_index` |
| TC-CLI-006 | `latest` selects the most recent workout     | Static test fixture ZIP with a most recent workout on `2025-05-04`.          | Call `--file tests/fixtures/export.zip running show latest`.                            | The detail view is rendered for the chronologically last workout in the full running-workout list and shows its global run ID. | ✅ `tc_cli_006_show_latest_selects_most_recent_workout` |
| TC-CLI-011 | Show rejects filter-only flags               | Static test fixture ZIP with valid running workouts.                         | Call `--file tests/fixtures/export.zip running show --year 2024 4`.                     | Argument parsing fails because `--year` is not accepted by `running show`.                             | ✅ `tc_cli_011_show_rejects_filter_flags` |
| TC-CLI-007 | Records output renders all record categories | Static test fixture ZIP with running workouts up to marathon distance.       | Call `--file tests/fixtures/export.zip running records`.                                | The Markdown table includes rows for longest run, fastest 5k, fastest 10k, fastest half marathon, and fastest marathon, with units shown only in the header row. | ✅ `tc_cli_007_records_renders_expected_rows` |
| TC-CLI-008 | Records keep global run IDs under filters    | Static test fixture ZIP with 2024 workouts that correspond to global IDs `2` and `3`. | Call `--file tests/fixtures/export.zip running records --year 2024`.                    | Record winners are selected only from 2024 workouts, but the `Run ID` column still uses the global unfiltered 1-based index. | ✅ `tc_cli_008_records_keep_global_run_ids_under_filters` |
| TC-CLI-009 | Empty filtered records still render rows     | Static test fixture ZIP with no workouts in 2022.                            | Call `--file tests/fixtures/export.zip running records --year 2022`.                    | The records table is still rendered with all five rows, and every cell except `Record Type` uses placeholder values. | ✅ `tc_cli_009_records_render_all_placeholder_rows_when_filter_is_empty` |
| TC-CLI-010 | Records select long-distance winners         | Static test fixture ZIP with qualifying 10k, half marathon, and marathon runs in 2025. | Call `--file tests/fixtures/export.zip running records --year 2025`.                    | The records table selects the 2025 10k, half marathon, and marathon winners using their global run IDs. | ✅ `tc_cli_010_records_select_long_distance_winners` |
| TC-CLI-012 | Default file path uses current directory     | Static test fixture ZIP copied to `./export.zip` in the package root before invocation. | Call `running records` with no `--file` flag.                                           | The tool opens `./export.zip` from the current working directory and renders the records table successfully. | ✅ `tc_cli_012_defaults_file_to_export_zip_in_current_directory` |
| TC-CLI-013 | List filters by month within a year          | Static test fixture ZIP with runs in February, March, and April 2025.       | Call `--file tests/fixtures/export.zip running list --year 2025 --month 2`.            | Output includes only the February 2025 workouts for the requested year.                                | ✅ `tc_cli_013_list_filters_by_month_within_year` |
| TC-CLI-014 | Month requires year for records              | Static test fixture ZIP with valid running workouts.                         | Call `--file tests/fixtures/export.zip running records --month 2`.                      | Argument parsing fails because `--month` requires `--year`.                                            | ✅ `tc_cli_014_records_require_year_when_month_is_present` |
| TC-CLI-015 | Month rejects out-of-range values            | Static test fixture ZIP with valid running workouts.                         | Call `--file tests/fixtures/export.zip running list --year 2025 --month 13`.           | Argument parsing fails because month values must be in the range `1..=12`.                             | ✅ `tc_cli_015_list_rejects_month_out_of_range` |

### Parsing And Data Extraction

| ID          | Description                                         | Precondition                                                                                          | Actions                                                            | Expected Result                                                                                                               | Automated |
| ----------- | --------------------------------------------------- | ----------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------- | --------- |
| TC-PARSE-001 | Child stats, metadata, and XML entities are honored | Static test fixture ZIP whose first workout has conflicting `totalDistance`, child statistics, metadata, and escaped attribute values. | Call `--file tests/fixtures/export.zip running show 1`.            | The detail view uses the child distance, shows energy, reports outdoor environment, and prints unescaped source and device. | ✅ `tc_parse_001_show_uses_child_stats_metadata_and_unescaped_attributes` |
| TC-PARSE-002 | Self-closing running workout without GPX is supported | Static test fixture ZIP whose second workout is a self-closing running workout using miles and no route file. | Call `--file tests/fixtures/export.zip running show 2`.            | The detail view shows the converted kilometre distance and prints `No GPS data available for splits.`                       | ✅ `tc_parse_002_show_reports_no_gps_for_self_closing_workout` |
| TC-PARSE-003 | Energy conversion and manual/indoor metadata are shown | Static test fixture ZIP whose fourth workout stores energy in `kcal` and includes `HKIndoorWorkout` and `HKWasUserEntered`. | Call `--file tests/fixtures/export.zip running show 4`.            | The detail view converts energy to kilojoules and shows both the indoor label and manual-entry note.                        | ✅ `tc_parse_003_show_converts_kcal_and_reports_manual_indoor_workout` |

### Computed Metrics And Output

| ID          | Description                                        | Precondition                                                                 | Actions                                                 | Expected Result                                                                                                             | Automated |
| ----------- | -------------------------------------------------- | ---------------------------------------------------------------------------- | ------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- | --------- |
| TC-COMP-001 | Splits include average HR and a partial final km   | Static test fixture ZIP whose first workout has GPX points for 2.5 km and overlapping HR records. | Call `--file tests/fixtures/export.zip running show 1`. | The splits table header includes `Split (km)`, `Pace (min/km)`, and `Avg HR (bpm)`, renders two full kilometres plus a `3~` partial split, and shows an extrapolated pace for the partial kilometre. | ✅ `tc_comp_001_show_renders_hr_splits_and_partial_final_km` |
| TC-COMP-002 | Splits omit HR column when no HR data is available | Static test fixture ZIP whose third workout has GPX data but no overlapping HR records.             | Call `--file tests/fixtures/export.zip running show 3`. | The splits table is rendered with unit-bearing headers and without the HR column.                                          | ✅ `tc_comp_002_show_omits_hr_column_when_no_hr_exists` |
| TC-COMP-003 | Records break pace ties by earliest run            | Static test fixture ZIP with two qualifying 5k-or-longer runs that share the same average pace.     | Call `--file tests/fixtures/export.zip running records`. | The faster-by-tie 5k record is assigned to the earliest chronological run among the tied workouts.                       | ✅ `tc_comp_003_records_break_pace_ties_by_earliest_run` |
